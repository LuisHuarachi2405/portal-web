query GetProducts($productsId: BUFFER!) {
  products(id: $productsId) {
    code
    name
    shortName
    idProduct
    idProductType
    productType
    idEntitySupplier
    supplierName
    inventoryFlag
  }
}
